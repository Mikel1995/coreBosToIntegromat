{
  "qs": {
    "operation": "listtypes",
    "sessionName": "{{connection.sessionName}}"
  },
  "url": "{{parameters.corebosurl}}/webservice.php",
  "method": "GET",
  "response": {
    "output": {
      "label": "{{item}}",
      "value": "{{item}}"
    },
    "iterate": "{{body.result.types}}"
  }
}
